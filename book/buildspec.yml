version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Move to frontend directory"
      - cd frontend
      - npm install

  build:
    commands:
      - echo "Build Next.js project (static export)"
      - npm run build

artifacts:
  base-directory: frontend/out
  files:
    - '**/*'
